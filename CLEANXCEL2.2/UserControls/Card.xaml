<UserControl x:Class="CLEANXCEL2._2.UserControls.Card"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CLEANXCEL2._2.UserControls"
             Name="userControl"
             mc:Ignorable="d" 
             HorizontalAlignment="Right"
             Width="550"
             Margin="0,0,10,1"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="0" Background="{StaticResource PrimaryColor}">
        <StackPanel Grid.Column="1" HorizontalAlignment="Stretch"
                    Margin="20,0" VerticalAlignment="Top">
            <TextBlock Grid.Row="0" Style="{StaticResource H5}" 
                       Text="{Binding Equipment, ElementName=userControl}" TextAlignment="Left" Margin="40,21,40,0"
                       Foreground="{StaticResource CWhite}"/>
            <TextBlock Grid.Row="1" Style="{StaticResource Label}" 
                       TextAlignment="Justify" VerticalAlignment="Top" Margin="40,13,40,21"
                       TextWrapping="WrapWithOverflow" LineHeight="32px" Text="{Binding Description, ElementName=userControl}"/>
        </StackPanel>
    </Grid>
</UserControl>
